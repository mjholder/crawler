# Session — 2026-04-24

**Project:** Crawler (Godot 4)
**Phase:** Core Systems
**Session length:**

---

## Goals for this session

- [ ]

## What I worked on

- Fixed inventory being locked during rest and shop nodes — `allows_inventory = true` added to `rest_event.tscn` and `shop_event.tscn`. The opt-in flag already existed on `Event` (event.gd:17) and `game.gd` already had the unlock path; the scenes just weren't opting in.

## Outcomes

### Completed
- Inventory unlocked during rest and shop events

### Incomplete / deferred
-

## Key decisions made

- Kept the opt-in `allows_inventory` flag model rather than inverting to "lock only combat" — safer for future event subclasses.

## Blockers / struggles

## Next session

- [x] Replace DialoguePanel — currently instantiates choice buttons dynamically with `_choices_container.add_child()`; replace with pre-built fixed slots (show/hide pattern) - Not doing this. We are sticking with the programatic instantiation of a premade scene.
- [x] Replace ShopPanel — `_rebuild_list()` uses `Button.new()` loop for item rows; replace with scene-based row template
