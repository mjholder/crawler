# Session — 2026-04-22

**Project:** Crawler (Godot 4)
**Phase:** Core Systems
**Session length:** <!-- e.g. 2h -->

---

## Goals for this session

- [x] Wire `set_dungeon_locked()` call-site in `game.gd`

## What I worked on

- Wired dungeon lock into `game.gd`: `_enter_dungeon()` now calls `player.inventory.set_dungeon_locked(true)` and `_gui.set_dungeon_locked(true)`; `_exit_dungeon()` calls both with `false`. Guard-clauses in `inventory.gd` and the `InventoryPanel` UI lock were already in place from the 4/21 session — this was the missing call-site.

## Outcomes

### Completed
- Dungeon lock fully wired: inventory data layer + UI panel lock/unlock on dungeon entry/exit

### Incomplete / deferred
-

## Key decisions made

- Lock point is `_enter_dungeon` / `_exit_dungeon` in `game.gd` — the natural boundary already used for game state and event config setup.

## Blockers / struggles

-

## Next session

- [ ] Verify dungeon lock in-engine: InventoryPanel read-only during dungeon; consumable use still works
- [ ] Verify buff consumable: `get_effective_stat()` reflects bonus; decrements each player turn; expires cleanly
- [ ] **Replace programmatic UI node creation with PackedScene instantiation or editor layout** — raw `Node.new()` / `Button.new()` sites:
  - `inventory_panel.gd` — equipment slot buttons, ring buttons, bag grid buttons
  - `consumable_belt_ui.gd` — belt slot buttons
  - `dialogue_panel.gd` — choice buttons
  - `shop_panel.gd` — item list buttons
  - `level_up_panel.gd` — per-stat rows (HBoxContainer + Label + two Buttons)
- [ ] Design and build `PickupChoicePanel` (blocked on loot system)
- [ ] Decide on next system to build (loot drops, player abilities, procedural floors?)
