# Session — 2026-02-28

**Project:** Crawler (Godot 4)
**Phase:** Prototyping
**Session length:** <!-- e.g. 2h -->

---

## Goals for this session

- [ ] Basic UI layer (health bars, combat log, action menus)
- [ ] Player sprite
- [x] Enemy sprite — skeleton model sourced, rigging next

## What I worked on

Found a skeleton model to use for enemy sprites.

## Outcomes

### Completed
- Sourced skeleton model for enemy sprites

### Incomplete / deferred
- Rig skeleton model and create the required poses for in-game use

## Key decisions made

- Skeleton is the first concrete enemy sprite asset — sets the visual style baseline for enemies

## Blockers / struggles

- Need to rig the model before any poses (idle, attack, hurt, death, etc.) can be produced

## Next session

- [ ] Rig skeleton model
- [ ] Define which poses are needed (idle, attack, hurt, death at minimum)
- [ ] Export poses/frames for use as sprites in Godot

---

## Carried over from 2026-02-27

Last session landed the event architecture. `event.gd` is the base class with a `SETUP → RUNNING → RESOLUTION → COMPLETE` phase state machine and virtual hooks (`_on_setup`, `_on_running`, `_on_resolution`). `game.gd` was updated to hold `current_event`, expose `start_event()`, and respond to the `event_complete` signal — it stays blind to internal phase state.

Still outstanding from that session:
- `CombatEvent` concrete implementation (enemies, turn signals, wave loop)
- `event_complete` result payload shape (loot, XP, player state delta)
