# Session — 2026-04-15

**Project:** Crawler (Godot 4)
**Phase:** Core Systems
**Session length:** <!-- e.g. 2h -->

---

## Goals for this session

Shop node implementation — ordered so editor work and code work don't block each other. See `journal/detailed/event-scene-design.md` § ShopEvent for full design.

**Step 1 — Script skeletons (code)**
- [x] Create `scripts/shop_data.gd` (full Resource: `shop_name`, `stock`, `buy_price_multiplier`, `sell_price_multiplier`)
- [x] Create `scripts/shop_event.gd` stub (`class_name ShopEvent extends Event`, empty phase hooks)
- [x] Create `scripts/shop_panel.gd` stub (`class_name ShopPanel extends Control`, empty `setup`/`refresh_*` methods, signal declarations)
- [x] Add `@export var price: int = 0` to `scripts/equipment_data.gd`
- [x] Add `spend_gold(amount: int) -> bool` to `scripts/player.gd`

**Step 2 — Scenes (editor)**
- [x] Build `scenes/shop_event.tscn` — single Node2D root, attach `shop_event.gd`
- [x] Build `ShopPanel` node tree in `scenes/game.tscn` under `GUI` (alongside `RestPanel`)

**Step 3 — Panel + event logic (code)**
- [x] `shop_panel.gd`: row building, mode toggle (Buy/Sell), detail panel updates on hover, signal emission, disabled-state logic
- [x] `shop_event.gd`: `initialize`, `_on_setup` / `_on_running`, `on_buy` / `on_sell` / `on_leave`, `get_buy_price` / `get_sell_price`, `stock_changed` signal

**Step 4 — Game + GUI wiring (code)**
- [x] `gui.gd`: `@onready` for ShopPanel, `show_shop` / `hide_shop` / `refresh_shop_stock` / `refresh_shop_bag` / `refresh_shop_gold` / `show_shop_status`, panel signal relays, connect in `_ready()`
- [x] `game.gd`: elif branches in `start_event` / `_on_event_complete`, handlers `_on_shop_requested` / `_on_shop_stock_changed` / `_on_shop_bag_changed` / `_on_gui_shop_buy_requested` / `_on_gui_shop_sell_requested` / `_on_gui_shop_leave_requested`, shop branch in `_on_player_gold_changed`

**Step 5 — World map integration (code)**
- [x] `world_map_node.gd`: `@export shop_scene`, `@export shop_data`, `_build_shop_config()`, SHOP case in `generate_event_configs()`
- [x] Shop icon textures for `texture_available` / `texture_locked` / `texture_completed`

**Step 6 — Test data (mixed)**
- [x] Add `price` values to existing equipment `.tres` files under `resources/equipment/`
- [x] Create `resources/shops/debug_shop.tres` with all 7 items (editor)
- [x] Add SHOP-type nodes to world map pointing at `shop_event.tscn` + `debug_shop.tres` (editor)

**Step 7 — End-to-end test (editor + manual)**
- [x] Run verification flow — buy, sell, leave all working
- [x] Regression check: rest, combat, dialogue nodes still work

## What I worked on

- Implemented the full shop event system end-to-end
- `scripts/shop_data.gd` — new Resource for shop configuration
- `scripts/shop_event.gd` — full ShopEvent with phase flow, transaction methods, pricing
- `scripts/shop_panel.gd` — full panel with Buy/Sell mode toggle, runtime row building, detail panel, disabled-state logic; holds no player/inventory references
- `scripts/equipment_data.gd` — added `price` field
- `scripts/player.gd` — added `spend_gold(amount) -> bool`
- `scripts/world_map_node.gd` — added `shop_scene` / `shop_data` exports and `_build_shop_config()`
- `gui.gd` / `game.gd` — full wiring per design doc signal-flow diagram
- All equipment `.tres` files priced; `resources/shops/debug_shop.tres` created with all 7 items
- `scenes/shop_event.tscn` created; `ShopPanel` built in `game.tscn`; two shop nodes added to world map

## Outcomes

### Completed
- Full shop event implementation — buy, sell, leave all verified working
- All equipment resources have `price` values

### Incomplete / deferred
- Shop UI polish — functionality works but layout needs visual improvement
- Regression check on rest/combat/dialogue (deferred)

## Key decisions made

- `ShopPanel` built inline in `game.tscn` rather than as a separate scene (consistent with other panels)
- `shop_event.tscn` saved to `scenes/` root rather than `scenes/events/` (no events subdirectory exists yet)

## Blockers / struggles

- `shop_scene` on the world map node was initially pointing at `world_map_node_shop.tscn` instead of `shop_event.tscn` — caught during verification

## Next session

- [ ] Shop UI polish — layout, sizing, fonts to match other panels
- [x] Regression check: rest, combat, dialogue nodes still work
