# Session — 2026-04-11

**Project:** Crawler (Godot 4)
**Phase:** Core Systems
**Session length:** <!-- e.g. 2h -->

---

## Goals for this session

- [ ] Design a class system for player characters
- [ ] Build a character creation screen (class selection + name input)
- [ ] Integrate character creation into game start flow

## What I worked on

- Detailed design session for player classes and leveling system — wrote `journal/detailed/player-classes-and-leveling.md`

## Outcomes

### Completed
- [x] Design a class system for player characters — full design doc covering PlayerClassData resource, leveling, XP curve, character creation flow, level-up UI

### Incomplete / deferred
- Character creation screen and integration (implementation not started)

## Key decisions made

- Player classes are pure data (`PlayerClassData` resource) — no code per class
- `max_health` is derived: `(CON * health_modifier) + class_health_bonus`, no longer an `@export`
- `base_damage` removed from Player — unarmed weapon resource will handle it
- XP curve: `xp_base * pow(xp_growth_factor, level - 1)` with exported tuning vars
- Level-ups grant 3 free distributable points + class auto-growth bonuses
- Level-up UI shown between events (end of `_on_event_complete`), not mid-combat
- Starting equipment fully equipped (not in bag) — see detailed design doc

## Blockers / struggles

-

## Next session — Implementation Order

- [ ] 1. Create `PlayerClassData` resource script (`scripts/player_class_data.gd`)
- [ ] 2. Add leveling state and logic to `player.gd` — `level`, `pending_stat_points`, `xp_to_next_level()`, `_level_up()`, `_apply_growth_rates()`, `spend_stat_point()`
- [ ] 3. Rework `max_health` on Player — remove `@export`, add `health_modifier` export, implement `_recalculate_max_health()`
- [ ] 4. Remove `base_damage` from Player and update `_calculate_damage()` to be weapon-only
- [ ] 5. Add `initialize(name, class_data)` and `_setup_starting_equipment()` to Player
- [ ] 6. Create at least one `PlayerClassData` `.tres` file (warrior) under `resources/classes/`
- [ ] 7. Build the character creation screen (scene + script) — name input, class picker, class description/stats preview, continue button
- [ ] 8. Wire character creation into GUI — `character_created` signal replaces `start_requested`
- [ ] 9. Update `game.gd` — connect `character_created`, remove old `_setup_starting_equipment()`, call `player.initialize()`
- [ ] 10. Build the level-up panel (scene + script) — stat display, +/− buttons, points counter, confirm
- [ ] 11. Wire level-up flow into `game.gd._on_event_complete()` — check `pending_stat_points`, show panel, resume on confirm
- [ ] 12. Create the unarmed weapon `.tres` and add auto-equip logic when no weapon is present
